FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev)
RUN npm install

# Copy source code
COPY src ./src

# Expose WebSocket port
EXPOSE 3000

# Start server with hot reload
CMD ["npm", "run", "dev"]
