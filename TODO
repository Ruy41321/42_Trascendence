DONE remove the button Play vs ai

FIXED back-to-lobby after match doesnt notify other clients
FIXED its not possible to start a match after an old one is finished (status staying as finished)
    only when clicked back-to-lobby, on play again its normal back-to-lobby
FIXED Vote to abbandon not working
FIXED When exiting a match with vote to abbandon the view of the queued player and spectators doesn reset

DONE:   Remove the "insert name" logic and add the login page to play

FIXED:  Removed old uncomplete PWA files causing warnings and errors on the console
        Resolved icon & favicon warning/error
        Resolve ssl cert error

Game backend:

    The backend thinks that there are always 4 players playing if only 2 plays , the other 2 are signed as Anonymous and they can also win, i think this is realted 
        to the issue where we see typed "DISCONNECTED" on the side of missing players.

    Play again and backToLobby when a match is finished do the same thing (return to lobby) instead play again should restart the match immediately.

    If a player crash (disconnect reloading the page) the game freeze without a way to restart (The view needs an update of GameState with status = "paused" to show 
        the message)

    If a match freeze there is no way to reset it, there should be a timer where after 20/30 seconds of no-activity reset the lobby allowing other players to play. This
        should work the same after a match finish if none click play again or back to lobby for those amount of seconds;

    A player can join mid match at the moment taking the place of one of the anonymous (it should get fixed , fixing the previous issue)

    A fifth player can join atm(it will get limited by the frontend but in case he force it it should be taken as spectactor)
    And send the info with current players at the first moment a player connects so he nows from the begin the current waiting or playing players and the client 
        hide the join button if already 4

    Change the logic to accept user in lobby: atm check if the is already taken, but it should check the token

    Use the abdallah's backend api to save match datas 

General:

    Privacy policy and terms of service
    Use https for internal traffic
    Do the subject requested Readme.md